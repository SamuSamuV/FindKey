using UnityEngine;

public class Enums : MonoBehaviour
{
    public enum Direction
    {
        Left,
        Right,
        Straight
    }

    public enum Actions
    {
        Look,
        Pick,
        Atack,
        Run,
    }
}
